package com.ua.demoswing.working;

/**
 *
 * @author User
 */
public class Serilyse {

    private void exportDate() {
      /*  File file = new File("Person.txt");
        try {
            FileWriter fw = new FileWriter(file);
            BufferedWriter bw = new BufferedWriter(fw);
            for (int i = 0; i < TableNameEmail.getRowCount(); i++) {
                for (int j = 0; j < TableNameEmail.getColumnCount(); j++) {
                    bw.write(TableNameEmail.getValueAt(i, j).toString());
                }
            }
            bw.close();
            fw.close();

        } catch (IOException exception) {
            exception.getMessage();
        }*/
    }
    
    
}
